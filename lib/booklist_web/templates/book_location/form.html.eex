<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :call_number %>
  <%= textarea f, :call_number %>
  <%= error_tag f, :call_number %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
